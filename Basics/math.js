// function add(num1, num2) {
// 	return num1 + num2;
// }

// function sub(num1, num2) {
// 	return num1 - num2;
// }

/** Modules */

// module.exports = add
// module.exports = sub // Overrides the previous value from add to sub

// module.exports = {
// 	add,
// 	subFn: sub,
// };

// exports.add = (a, b) => a + b; // These are anonymous funcs
// exports.sub = (a, b) => a - b;
